# ========================================
# Next-web 生产环境配置示例
# ========================================
# 
# ⚠️ 重要提示：
# 1. 此文件仅作为示例，不会被部署使用
# 2. 实际生产环境变量需要在 Cloudflare Pages 项目设置中配置
# 3. 不要在此文件中填写真实的生产环境配置
#
# ========================================

# ========================================
# 🔧 必填：后端API配置
# ========================================

# 后端API地址（必填）
# ⚠️ 生产环境必须使用 HTTPS 协议
# 示例: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# 站点ID（必填）
# 从管理后台获取你的站点ID
NEXT_PUBLIC_SITE_ID=1

# API访问密钥（可选）
# 如果后端启用了API密钥验证，需要配置此项
NEXT_PUBLIC_API_KEY=

# ========================================
# 🔒 重新验证密钥（推荐配置）
# ========================================
# 用于ISR（增量静态再生成）的安全密钥
# 后端更新内容后，会调用前端的 /api/revalidate 接口
# ⚠️ 生产环境必须使用强密钥（建议使用随机生成的长字符串）
REVALIDATE_SECRET=your-production-secret-key-here

# ========================================
# 📊 可选：数据分析配置
# ========================================

# Google Analytics ID
NEXT_PUBLIC_GA_ID=

# Google Tag Manager ID
NEXT_PUBLIC_GTM_ID=

# ========================================
# 🌐 可选：自定义域名
# ========================================

# 自定义域名（用于生成绝对URL）
# 如果配置了自定义域名，填写此项
# 示例: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ========================================
# 📝 部署说明
# ========================================
#
# Cloudflare Pages 部署环境变量配置步骤：
#
# 1. 登录 Cloudflare Dashboard (https://dash.cloudflare.com)
# 2. 进入你的 Pages 项目
# 3. 点击 Settings → Environment variables
# 4. 在 Production 环境中添加以下变量：
#    - NEXT_PUBLIC_API_URL (必填)
#    - NEXT_PUBLIC_SITE_ID (必填)
#    - REVALIDATE_SECRET (推荐)
#    - 其他可选变量
# 5. 保存后，在 Deployments 页面点击 Retry deployment 重新部署
#
# Preview 环境（可选）：
# - 可以配置与 Production 相同的值
# - 或者配置测试环境的 API 地址
#
# ========================================
