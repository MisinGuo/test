# 类型定义组织规范

## 📁 目录结构

```
src/config/
├── types/                    # 共享类型定义
│   ├── common.ts            # 通用类型（多语言、链接、SEO等）
│   ├── api.ts               # API 相关类型
│   └── index.ts             # 统一导出
├── pages/                   # 页面配置
│   ├── home/               # 首页模块
│   │   ├── types.ts        # 首页专属类型
│   │   ├── index.ts        # 导出配置和类型
│   │   └── ...
│   ├── list/               # 列表页模块
│   │   └── ...
│   └── index.ts            # 导出所有页面配置
└── site/                   # 站点配置
    ├── locales.ts          # 多语言配置
    └── types.ts            # 站点配置类型
```

## ✅ 设计原则

### 1. 就近原则（Colocation）

**类型定义应该靠近使用位置**

```typescript
// 按模块拆分
// src/config/pages/home/types.ts (100 lines)
// src/config/pages/list/types.ts (80 lines)
```

**优势：**
- 修改首页时只需关注 `home/` 目录
- 类型和实现在同一目录，易于理解
- 减少跨文件跳转，提升开发效率

### 2. 单一职责（Single Responsibility）
**每个类型文件只负责一个模块**

```typescript
// ✅ 好的实践
// home/types.ts - 只定义首页相关类型
export interface HeroConfig { ... }
export interface HomePageConfig { ... }

// list/types.ts - 只定义列表页相关类型
export interface ListPageConfig { ... }
export interface FilterConfig { ... }
```

### 3. 类型复用（Type Reuse）
**通用类型抽取到 `types/common.ts`**

```typescript
// types/common.ts
export type LocalizedString = string | Record<string, string>
export type LocalizedArray<T> = T[] | Record<string, T[]>

// home/types.ts
import type { LocalizedString } from '../../types/common'

export interface HeroConfig {
  title: LocalizedString  // ✅ 复用通用类型
}
```

### 4. 清晰的依赖层级（Clear Dependencies）
```
types/common.ts     (最底层，无依赖)
    ↓
pages/home/types.ts (依赖 common)
    ↓
pages/home/index.ts (导出配置)
    ↓
app/[locale]/page.tsx (使用配置)
```

## 📝 命名规范

### 类型命名
- **接口**：使用 PascalCase + Config/Item/Options 后缀
  ```typescript
  interface HomePageConfig { }
  interface StatItem { }
  interface FilterOptions { }
  ```

- **类型别名**：使用 PascalCase
  ```typescript
  type LocalizedString = string | Record<string, string>
  type LayoutType = 'grid-3' | 'grid-4' | 'grid-6'
  ```

### 文件命名
- **类型文件**：`types.ts`
- **配置文件**：对应功能名，如 `home.ts`, `list.ts`
- **导出文件**：`index.ts`

## 🔍 导入规范

### 推荐的导入方式
```typescript
// ✅ 从模块索引导入
import type { HomePageConfig } from '@/config/pages/home'
import type { LocalizedString } from '@/config/types'

// ❌ 避免直接导入类型文件
import type { HomePageConfig } from '@/config/pages/home/types'
```

### 为什么使用索引文件？
1. **统一入口**：所有导入通过 `index.ts`，便于管理
2. **重构友好**：修改内部结构不影响外部引用
3. **更好的 Tree Shaking**：打包工具可以更好地优化

## 🎯 实战示例

### 添加新页面配置

1. **创建页面目录**
```bash
mkdir src/config/pages/detail
```

2. **定义类型**
```typescript
// src/config/pages/detail/types.ts
import type { SeoMetadata } from '../../types/common'

export interface DetailPageConfig {
  metadata: SeoMetadata
  // ... 其他配置
}
```

3. **创建配置**
```typescript
// src/config/pages/detail/index.ts
export * from './types'

export const detailConfig: DetailPageConfig = {
  // ... 配置内容
}
```

4. **使用配置**
```typescript
// app/[locale]/detail/page.tsx
import type { DetailPageConfig } from '@/config/pages/detail'
```

##  参考资料

- [TypeScript Handbook - Modules](https://www.typescriptlang.org/docs/handbook/modules.html)
- [Google TypeScript Style Guide](https://google.github.io/styleguide/tsguide.html)
- [React TypeScript Cheatsheet](https://react-typescript-cheatsheet.netlify.app/)
