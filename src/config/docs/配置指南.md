# Next-web 配置指南

## 📋 快速开始

### 1. 环境准备

确保你已安装：
- Node.js 18.x 或更高版本
- pnpm 8.x 或更高版本（推荐）或 npm

### 2. 安装依赖

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install
```

### 3. 配置后端连接

**步骤 1：创建环境变量文件**

```bash
# 复制环境变量模板
cp .env.example .env.local
```

**步骤 2：填写配置**

编辑 `.env.local` 文件：

```env
# 后端API地址（必填）
NEXT_PUBLIC_API_URL=http://localhost:8080

# 站点ID（必填，从管理后台获取）
NEXT_PUBLIC_SITE_ID=1

# API密钥（可选）
NEXT_PUBLIC_API_KEY=
```

> 💡 **如何获取站点ID？**
> 1. 登录管理后台
> 2. 进入"站点管理"菜单
> 3. 查看你的站点详情
> 4. 复制站点ID

### 4. 启动开发服务器

```bash
# 开发模式
pnpm dev

# 或
npm run dev
```

访问 http://localhost:3000

---

## 🎨 自定义配置

### 站点信息配置

编辑 `src/config/site.ts`：

```typescript
export const siteConfig = {
  // 网站名称
  name: '我的游戏站',
  
  // 网站描述（SEO）
  description: '最全游戏折扣聚合平台',
  
  // 网站域名
  hostname: 'https://mygame.com',
  
  // Logo文件路径
  logo: '/logo.svg',
  
  // Favicon
  favicon: '/favicon.ico',
  
  // SEO关键词
  keywords: [
    '游戏盒子',
    '游戏折扣',
    '首充折扣',
  ],
  
  // 社交媒体链接
  social: {
    github: 'https://github.com/yourname',
    twitter: 'https://twitter.com/yourname',
  },
  
  // 功能开关
  features: {
    search: true,      // 搜索功能
    darkMode: true,    // 深色模式
    comments: false,   // 评论系统
    analytics: true,   // 数据分析
  },
  
  // 导航配置
  nav: [
    { text: '首页', link: '/' },
    { text: '游戏', link: '/games' },
    { text: '盒子', link: '/boxes' },
    { text: '攻略', link: '/strategy' },
  ],
}
```

### 主题配置

编辑 `src/config/theme.ts`：

```typescript
export const themeConfig = {
  colors: {
    light: {
      primary: '#3B82F6',     // 主色调
      secondary: '#8B5CF6',   // 辅助色
      accent: '#F59E0B',      // 强调色
      // ...更多颜色
    },
    dark: {
      primary: '#60A5FA',
      // ...暗色模式配置
    },
  },
  
  fonts: {
    sans: ['Inter', 'system-ui', 'sans-serif'],
    // ...字体配置
  },
  
  layout: {
    maxWidth: '1440px',       // 最大宽度
    contentWidth: '960px',    // 内容宽度
    sidebarWidth: '280px',    // 侧边栏宽度
    headerHeight: '64px',     // 头部高度
  },
  
  logo: {
    light: '/logo-light.svg',  // 亮色Logo
    dark: '/logo-dark.svg',    // 暗色Logo
    height: 40,                // Logo高度
  },
}
```

### 后端API配置

⚠️ **不要直接修改** `src/config/backend.ts`！

通过 `.env.local` 文件配置：

```env
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NEXT_PUBLIC_SITE_ID=1
NEXT_PUBLIC_API_KEY=your-api-key
```

---

## 🖼️ 自定义资源

### 替换Logo

1. 准备你的Logo文件（SVG或PNG格式）
2. 放入 `public/` 目录
3. 更新 `src/config/site.ts`：

```typescript
export const siteConfig = {
  logo: '/my-logo.svg',
  favicon: '/my-favicon.ico',
}
```

### 添加自定义图片

将图片放入 `public/assets/img/` 目录，然后在代码中引用：

```tsx
<Image src="/assets/img/banner.jpg" alt="Banner" />
```

---

## 📐 目录结构

```
Next-web/
├── .env.local              # 环境变量（需自行创建）
├── .env.example            # 环境变量模板
├── package.json
├── next.config.js          # Next.js配置
├── tailwind.config.js      # Tailwind配置
│
├── public/                 # 静态资源
│   ├── logo.svg           # Logo文件
│   ├── favicon.ico        # Favicon
│   └── assets/            # 其他资源
│       ├── img/           # 图片
│       └── icon/          # 图标
│
└── src/
    ├── app/               # Next.js页面
    │   ├── page.tsx      # 首页
    │   ├── articles/     # 文章页面
    │   ├── games/        # 游戏页面
    │   └── boxes/        # 盒子页面
    │
    ├── components/        # React组件
    │   ├── layout/       # 布局组件
    │   ├── article/      # 文章组件
    │   └── ui/           # UI组件
    │
    ├── config/           # 配置文件 ⭐ 主要修改这里
    │   ├── site.ts       # 站点配置
    │   ├── theme.ts      # 主题配置
    │   ├── backend.ts    # 后端配置
    │   └── index.ts      # 统一导出
    │
    └── lib/              # 工具库
        ├── api.ts        # API客户端
        └── utils.ts      # 工具函数
```

---

## 🚀 部署到Cloudflare Pages

### 方式1: GitHub Actions自动部署（推荐）

1. Fork本项目到你的GitHub
2. 修改配置文件（site.ts, theme.ts）
3. 配置GitHub Secrets：
   - `CLOUDFLARE_API_TOKEN`
   - `CLOUDFLARE_ACCOUNT_ID`
4. 推送到main分支，自动触发部署

### 方式2: Cloudflare Pages直接连接

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 Pages
3. 点击 "Create a project"
4. 连接你的GitHub仓库
5. 配置构建设置：
   ```
   Framework preset: Next.js
   Build command: pnpm build
   Build output directory: .next
   Node version: 18
   ```
6. 添加环境变量：
   ```
   NEXT_PUBLIC_API_URL=https://api.yourdomain.com
   NEXT_PUBLIC_SITE_ID=1
   NEXT_PUBLIC_API_KEY=your-api-key
   REVALIDATE_SECRET=your-secret
   ```
7. 点击 "Save and Deploy"

---

## 📊 数据分析配置

### Google Analytics

1. 创建GA4属性：https://analytics.google.com/
2. 获取测量ID（格式：G-XXXXXXXXXX）
3. 在 `.env.local` 中配置：

```env
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
```

---

## 💬 评论系统配置

### Giscus（基于GitHub Discussions）

1. 访问 https://giscus.app/zh-CN
2. 按照引导配置
3. 复制生成的配置到 `.env.local`：

```env
NEXT_PUBLIC_GISCUS_REPO=yourname/repo
NEXT_PUBLIC_GISCUS_REPO_ID=R_xxxxx
NEXT_PUBLIC_GISCUS_CATEGORY=Announcements
NEXT_PUBLIC_GISCUS_CATEGORY_ID=DIC_xxxxx
```

---

## 🔍 常见问题

### Q1: 修改配置后没有生效？

**A**: 修改 `.env.local` 或配置文件后，需要重启开发服务器：
```bash
# 停止服务器（Ctrl+C）
# 重新启动
pnpm dev
```

### Q2: 如何更换主题颜色？

**A**: 编辑 `src/config/theme.ts`，修改 `colors` 配置：
```typescript
colors: {
  light: {
    primary: '#your-color',  // 你的颜色
  }
}
```

### Q3: Logo显示不正常？

**A**: 检查：
1. 文件是否放在 `public/` 目录
2. `site.ts` 中的路径是否正确
3. 文件名是否匹配（区分大小写）

### Q4: API请求失败？

**A**: 检查：
1. `.env.local` 中的 `NEXT_PUBLIC_API_URL` 是否正确
2. 后端服务是否正常运行
3. 站点ID是否正确
4. 打开浏览器控制台查看详细错误

### Q5: 构建失败？

**A**: 常见原因：
1. Node.js版本过低（需要18.x+）
2. 依赖安装不完整：删除 `node_modules` 和 `pnpm-lock.yaml`，重新安装
3. TypeScript类型错误：检查配置文件的类型定义

---

## 📞 技术支持

- **文档**: [完整文档](./docs/README.md)
- **API文档**: [API接口文档](./docs/API接口文档.md)
- **问题反馈**: [GitHub Issues](https://github.com/your-org/Next-web/issues)

---

## 🎉 开始使用

现在你已经了解了基本配置，可以开始自定义你的网站了！

1. ✅ 配置环境变量
2. ✅ 修改站点信息
3. ✅ 自定义主题颜色
4. ✅ 替换Logo和图标
5. ✅ 部署到Cloudflare Pages

祝你使用愉快！🚀
